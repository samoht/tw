(test
 (name test)
 (libraries
  tw
  alcotest
  re
  astring
  fmt
  cmdliner
  unix
  tw_tools
  cascade
  cascade.diff
  test_helpers)
 ; test_surface reads the library's own interfaces to check that every utility
 ; they build is published on the flat surface.
 (deps
  upstream/utilities.txt
  upstream/variants.txt
  (glob_files ../lib/*.mli)))
